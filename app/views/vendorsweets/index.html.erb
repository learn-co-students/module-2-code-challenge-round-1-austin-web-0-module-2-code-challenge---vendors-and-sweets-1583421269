
<%= form_for @vendor_sweet do |x| %>
    <%= x.collection_select :vendor_id, Vendor.all, :id, :name%>
    <%= x.collection_select :sweet_id, Sweet.all, :id, :name%>
    <%= x.number_field :price %>
    <%= x.submit "Submit"%>
    <%end%>